"use client"; // Ensures it's a client component in Next.js

const AdminNavbar = () => {
    return (
        <nav className="navbar">
            <div className="nav-container">
                <h1 className="logo">VenueEase</h1>
            </div>
        </nav>
    );
};

export default AdminNavbar;